// general mixins

wrapper($max-width=960px)
  cf()
  center($max-width)
  //padding 0 lines(1)


sticky()
  position fixed
  top 0
  left 0
  right 0
  margin 0


link($color=$primary-text-color, $bg=transparent)
  &,
  &:hover,
  &:active,
  &:visited
    color $color
    background-color $bg
